#!/usr/bin/env bash
set -euo pipefail

# Add custom deploy commands here when the server needs extra setup.
# Examples:
# php artisan storage:link || true
# sudo apt-get update && sudo apt-get install -y imagemagick
# php artisan some:custom-command

echo "deploy-extra.sh: no custom commands configured"
